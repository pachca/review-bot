{
  "name": "review-bot",
  "module": "index.ts",
  "scripts": {
    "build": "./node_modules/.bin/esbuild index.ts  --minify --bundle --platform=node --target=node18 --outfile=index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.20.2",
    "octokit": "^3.2.0",
    "ts-expect": "^1.3.0",
    "wretch": "^2.8.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
